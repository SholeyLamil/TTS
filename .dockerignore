# Keep host build artifacts out of the image so the in-container restore is authoritative.
**/bin/
**/obj/
**/out/

# Local config / secrets / VCS
.env
*.env
!.env.example
.git/
.gitignore
.vs/
.vscode/
.idea/

# Tests aren't needed to publish the API image
tests/

# Docs
*.md
